Just drop the file onto either compress_24.bat (24 MB for Discord/Messenger), or compress_custom.bat which will ask you for the size you want
And it will be outputted into the same directory as your input with a sufix of _comp.mp4
Keep in mind that if such a file exists it will be replaced