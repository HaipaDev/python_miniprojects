Just drop the file onto run_vidclipper.bat
And it will be outputted into the same directory as your input with a sufix of _cut.mp4
If such a file exists the new one will get an additional suffix of _cut_1.mp4, _2.mp4 and so on